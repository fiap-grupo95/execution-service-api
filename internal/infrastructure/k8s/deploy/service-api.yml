apiVersion: v1
kind: Service
metadata:
  name: execution-service-api-service
  namespace: execution-service-api
spec:
  selector:
    app: execution-service-api
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer